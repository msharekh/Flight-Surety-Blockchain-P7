// function f1(x) {
//     console.log(x);
// }

function fffff(var1, callback) {
    // 1
    // 2
    // 3
    // 4
    // 5
    // 6
    callback(var1);
}

fffff('ha ha ha', (x) => {
    console.log(x);
});


//==========
/* (error) => {
    // display('Oracles', 'Trigger oracles', [{ label: 'Fetch Flight Status', error: error, value: result.flight + ' ' + result.timestamp }]);
    debugger;



}

function registerAirline(_airlineAddress, callback) {
    let self = this;
    debugger;
    self.flightSuretyApp.methods
        .registerAirline.call(_airlineAddress, callback());

    callback(_airlineAddress);
}

contract.registerAirline(_airlineAddress, fff); */